server:
  addr: ":8080"
  static_dir: "./web/dist"

auth:
  jwt_secret: "nasha-demo-secret"
  token_ttl: "24h"

database:
  dsn: "./nasha.db"

cache:
  dir: "./cache"

mounts:
  - name: "Demo"
    type: local
    path: /
    config:
      root: "./demo_data"
